# 工作人员管理接口

## 修改工作人员资料

- 方法与路径：`PUT /operator/staff/{staffId}`
- 身份要求：当前登录用户必须为景点运营者。
- 可修改字段：工作人员姓名、手机号。

请求示例：

```json
{
  "displayName": "张敏",
  "phone": "13800138000"
}
```

接口会限制目标账号为当前运营者所属景点的 `STAFF` 账号；登录账号、密码、角色、状态和所属景点不能通过此接口修改。
